SELECT A.ID, B.FISH_NAME, A.LENGTH
FROM (SELECT ID, FISH_TYPE, LENGTH
FROM FISH_INFO
WHERE (FISH_TYPE, LENGTH) IN
(SELECT FISH_TYPE, MAX(LENGTH)
FROM FISH_INFO
GROUP BY FISH_TYPE)) A JOIN 
FISH_NAME_INFO B ON A.FISH_TYPE = B.FISH_TYPE
